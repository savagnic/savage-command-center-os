# Prospect / outreach content — never commit (see analysis/BLOCK-A-APPLY.md, A6)
app.emails.private.js

# Prospect / outreach content — never commit

# Node modules
node_modules/
package-lock.json

# Logs & temp files
*.log
.DS_Store
